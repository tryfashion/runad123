# runad123 开发约定

## 先读什么

- 每轮先读 README.md 和 docs/IMPLEMENTATION_PLAN.md 的进度部分。
- 以 docs/ARCHITECTURE.md、DATABASE.md、CONTRACTS.md 为规格，按当前阶段读取有关章节。
- 用户已明确授权开始编码；当前阶段和待验证项见 IMPLEMENTATION_PLAN，逐个里程碑实施并汇报。

## 不得自行改变的要求

- TypeScript、pnpm workspace、React MV3 插件、Next.js 网站/API、Node 后台任务、MySQL 8。
- MySQL 为唯一必需数据库；首版不添加 Redis、消息中间件、向量库或微服务。
- 首版必须实现服务端 DeepSeek 标题/描述风险检查、匿名身份、账号体系及服务端登录开关。
- 首版只做当前商品；不增加广告库、销量推算、图片侵权、评论搬运或 Shopify Admin 直传。
- 不声称文本模型可以确认法律意义上的侵权或保证无侵权；检查失败不等于低风险。
- 最终文本改变必须使旧检查失效。插件里不存放 DeepSeek 密钥或数据库凭据。
- 不复制竞品实现；公开接口和实测行为作为参考。
- UI 默认跟随浏览器、支持手动切换并记忆；词典从 M0 接入。界面语言不得改变商品内容、目标国家或触发 AI 重检。
- 不在仓库、日志、测试快照中写入真实凭据、邮箱验证码或完整会话令牌。

## 实施方式

- 每次完成一个里程碑，包含该阶段必要测试，再更新实施文档。不要只搭大量空页面。
- 保持 API、共享 Zod schema、数据库约束一致；服务端不信任插件提交的用户 ID、角色、风险结果或哈希。
- 金额用十进制字符串；JSON 中外部商品/规格 ID 一律字符串。
- API 修改必须同步合同；数据库修改必须有迁移文件，不能让生产依赖自动 push schema。
- 文档按 README 的职责划分维护；字段变更进入主表/接口表，替换过时定义，不只追加补丁。复查记录是历史，不能覆盖现行合同。
- 选择 stable 依赖并锁定；不为追新自动更换 ORM、框架或大版本。
- 普通实现细节自主决定并记录。若需求确实冲突，指出具体冲突，不静默删减。
- 不需要为每个可逆文件修改重新征求确认；真实部署和收费服务开通不包含在编码范围内。

## 完成定义

- 当前阶段的验收成立，错误分支有证据，文档进度准确。
- 模拟、自动化、真实浏览器验证、真实 Shopify 导入分开报告。
- 缺少凭据时标记“待真实验证”，不得宣布项目已可生产上线。
